import { async, TestBed } from "@angular/core/testing";


describe('first tests', () => {
  it('should be true', () => {
    expect(true).toBe(true); // assert
  });

  it('should be false', () => {
    expect(false).toBe(false); //assert
  });
});
